name: One-Click Build x86_64

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出源码
        uses: actions/checkout@v4

      - name: 极致空间清理 (腾出30GB+)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: 安装编译环境
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget
          sudo apt-get autoremove --purge
          sudo apt-get clean

      - name: 拉取源码
        run: |
          git clone --depth 1 https://github.com/immortalwrt/immortalwrt openwrt

      - name: 更新并安装 Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 注入配置与初始化脚本
        run: |
          cd openwrt
          sed -i 's/192.168.1.1/192.168.10.1/g' package/base-files/files/bin/config_generate
          
          mkdir -p package/base-files/files/etc/uci-defaults
          cat <<EOF > package/base-files/files/etc/uci-defaults/99-init
          #!/bin/sh
          uci set unblockneteasemusic.@main[0].enabled='1'
          uci set unblockneteasemusic.@main[0].unblock_way='viasocksproxy'
          uci commit unblockneteasemusic
          exit 0
          EOF
          chmod +x package/base-files/files/etc/uci-defaults/99-init

      - name: 生成配置文件 (defconfig)
        run: |
          cd openwrt
          rm -rf tmp/
          cat <<EOF > .config
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y
          CONFIG_PACKAGE_luci-app-passwall=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-adguardhome=y
          CONFIG_PACKAGE_luci-app-mosdns=y
          CONFIG_PACKAGE_luci-app-unblockneteasemusic=y
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_luci-app-portainer=y
          CONFIG_PACKAGE_luci-app-alist=y
          CONFIG_PACKAGE_luci-app-filebrowser=y
          CONFIG_PACKAGE_luci-app-samba4=y
          CONFIG_PACKAGE_luci-app-tailscale=y
          CONFIG_PACKAGE_luci-app-zerotier=y
          CONFIG_PACKAGE_luci-theme-argon=y
          CONFIG_TARGET_KERNEL_PARTSIZE=256
          CONFIG_TARGET_ROOTFS_PARTSIZE=1024
          EOF
          make defconfig

      - name: 预下载依赖包
        run: |
          cd openwrt
          make download -j8
          find dl -size -1024c -exec rm -f {} \;
          make download -j8

      - name: 开始编译
        run: |
          cd openwrt
          df -h
          # 执行编译。如果失败，自动清理最占空间的 build_dir 目录并退出
          make -j$(nproc) || (rm -rf build_dir && make -j1 V=s)

      - name: 整理固件文件
        if: success()
        run: |
          cd openwrt/bin/targets/x86/64/
          rm -rf packages
          echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV

      - name: 上传固件
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-x86-64
          path: ${{ env.FIRMWARE_PATH }}/*-combined-efi.img.gz
          retention-days: 1

      - name: 编译失败清理
        if: failure()
        run: |
          df -h
          echo "编译失败，已自动触发临时文件清理以保护 GitHub 运行器磁盘。"
